# Production env vars that are safe to commit (publishable keys only).
# Vite reads this file automatically when running `vite build` in production mode.
#
# These values are EMBEDDED INTO THE BROWSER BUNDLE — they are public by design.
# Never put Razorpay key SECRET, Supabase service role key, or webhook secrets here.
# Those live only in Vercel → Settings → Environment Variables.

VITE_SUPABASE_URL="https://ylctrvnptuewnniyxncc.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="sb_secret_OSqONmHyDAtvp5YafmRkJw_4M0Gc7nv"
VITE_SUPABASE_PROJECT_ID="ylctrvnptuewnniyxncc"
VITE_RAZORPAY_KEY_ID="rzp_live_StvLfZlKF8WxWW"

# Use the Vercel serverless function on the same origin
VITE_API_WORKER_URL=""
