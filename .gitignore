# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment variables - NEVER commit your local secrets!
.env
.env.local
.env.*.local
supabase/.env.local

# Exception: production publishable keys are safe to commit (they are
# embedded in the browser bundle anyway, by design). Keep this file's
# contents limited to VITE_* values.
!.env.production

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
